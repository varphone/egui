Demonstrates the difference between multiple normal windows, a topmost window, and Foreground overlays.

```sh
cargo run -p topmost_windows
```

Use the controls in the center panel to toggle individual normal windows, then click and drag them to verify that they only reorder within the normal layer while the topmost window stays above them.
